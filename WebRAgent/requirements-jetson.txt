# Зависимости WebRAgent для Jetson (без Docker).
#
# Устанавливать В ОКРУЖЕНИЕ qwen-valera (после setup_jetson6.sh), чтобы не
# подтянуть второй torch:
#
#   /mnt/valera/conda-envs/qwen-valera/bin/pip install -r requirements-jetson.txt
#
# ВАЖНО:
#  * torch / torchvision / sentence-transformers здесь НЕ указаны — они уже
#    установлены Jetson-сборкой (см. ../requirements-jetson6.txt).
#  * docling (конвертация PDF/DOCX/PPTX) намеренно исключён: он тянет свой
#    torch и может заменить Jetson-сборку на CPU-версию. Без него приложение
#    работает, для PDF используется PyPDF2.
#  * openai / anthropic не нужны: используется локальная модель Qwen2.5-Omni.
#  * pymongo нужен для истории чатов и пользователей (см. ниже про MongoDB).

flask==3.1.0
Werkzeug==3.1.3
flask-login==0.6.3
python-dotenv==1.0.1
qdrant-client==1.13.3
requests==2.32.3
beautifulsoup4==4.13.3
PyPDF2==3.0.1
Pillow==11.1.0
markdown==3.7
python-docx==1.1.2
python-pptx==1.0.2
openpyxl==3.1.5
pandas==2.2.3
pymongo==4.11.2
html2text==2024.2.26
fake-useragent==2.0.3
dnspython==2.7.0

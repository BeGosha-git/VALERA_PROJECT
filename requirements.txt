# QWEN-VALERA dependencies — for JetPack 6.x (Python 3.10+, CUDA 12)
# Jetson ARM64 does not support flash-attn; we use sdpa attention.

# Core inference
transformers>=4.57.0        # Qwen3-Omni support (installed from GitHub ideally)
accelerate>=1.0.0
qwen-omni-utils>=0.1.0
soundfile>=0.13.1
torch>=2.5.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
websockets>=14.0

# Audio I/O
sounddevice>=0.5.0
pydub>=0.25.0
numpy>=1.26.0

# Database
sqlalchemy>=2.0.0
chromadb>=0.5.0

# Internet search
duckduckgo-search>=7.0.0
aiohttp>=3.10.0

# Utilities
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
loguru>=0.7.0
huggingface_hub>=0.24.0
requests>=2.31.0

# Основные зависимости
kairos-asr[cpu]>=0.7.0   # Распознавание речи (CPU-версия)
sounddevice>=0.5.0       # Запись с микрофона
silero-vad>=5.1          # Детектор голосовой активности (без pkg_resources)
soundfile>=0.12.0        # Сохранение аудио в WAV
numpy>=1.23.5            # Работа с массивами
scipy>=1.15.0            # Научные вычисления (нужна для noisereduce)
noisereduce>=3.0.0       # Шумоподавление

# Дополнительно (для работы с прокси, если нужно)
httpx[socks]>=0.28.0     # Поддержка SOCKS-прокси для скачивания моделей

# Если проблемы с setuptools (необязательно)
setuptools>=65.0.0